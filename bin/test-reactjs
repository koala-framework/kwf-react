#!/bin/sh

babelrc="./.babelrc"
if ! [ -f "$babelrc" ]
then
    echo "babelrc not found."
    exit;
fi

script="./vendor/bin/mocha"
if [ -f "$script" ]
then
    ./vendor/bin/mocha --compilers js:babel-register --recursive
else
    echo "$script not found. Please install mocha via npm."
fi
